query CartInput {
  cart {
    lines {
      id
      quantity
      attribute(key: "custom_price") {
        value
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
